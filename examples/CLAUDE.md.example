## 开发工作流规则（必须遵守）

所有开发任务，无论大小，都必须自动按以下 agent 流水线执行，不需要用户提醒：

### 完整流水线（从想法到上线）

```
用户想法/需求
    │
    ▼
┌─────────────────┐
│ product-manager  │  需求模糊或需要市场调研时启动
│ (产品经理 Agent) │  输出: docs/PRD-{产品名}-{日期}.md
└────────┬────────┘
         │ PRD 文档
         ▼
┌─────────────────────┐
│ feature-decomposer   │  将 PRD/需求拆解为功能组 + 里程碑
│ (功能拆解 Agent)     │  输出: docs/architecture/product-spec.md
│                      │        docs/test/requirements-traceability.md
└────────┬────────────┘
         │ 功能清单 + 架构方案
         ▼
┌────────────────────────────┐
│ code-reviewer (阶段一)      │  架构方案评审：程序设计/分布式/
│ (代码评审 Agent — 架构)     │  数据库/数据安全/文档完备性
│                             │  输出: docs/architecture/architecture-review.md
└────────┬───────────────────┘
         │ P0/P1 → 回退 decomposer 修方案；仅 P3 → 放行
         ▼
┌─────────────────────────┐
│ frontend-page-designer   │  基于功能规格出 UI/页面设计
│ (页面设计 Agent)         │  输出: 设计规格文档
└────────┬────────────────┘
         │ 设计规格
         ▼
┌─────────────────┐
│ module-coder     │  按功能清单顺序持续编码实现
│ (编码 Agent)     │  到里程碑节点暂停，先交技术评审
└────────┬────────┘
         │ 代码实现 + milestone 报告
         ▼
┌────────────────────────────┐
│ code-reviewer (阶段二)      │  代码评审：架构落地/事务一致性/
│ (代码评审 Agent — 代码)     │  数据安全/内存泄漏/结构分离
│                             │  输出: docs/test/code-review-M{N}.md
└────────┬───────────────────┘
         │ P0 → coder 立即修复
         │ P1/P2 → coder 修后必须重新走 QA
         │ 仅 P3 → 放行进入 QA
         ▼
┌────────────────────────────┐
│ design-parity-inspector     │  审核 UI 实现与设计规格的还原度
│ (设计还原审核 Agent)        │  输出: 设计一致性报告
└────────┬───────────────────┘
         │ 设计审核报告
         ▼
┌─────────────────┐
│ qa-inspector     │  里程碑节点做质量验收
│ (QA Agent)       │  验证验收标准，更新追踪清单
└─────────────────┘
```

### 各阶段触发规则

| 阶段 | Agent | 何时触发 | 输入 | 输出 |
|------|-------|---------|------|------|
| 0 | **product-manager** | 需求模糊、需要竞品分析、用户说"调研一下"、从零开始的新产品 | 用户的模糊想法 | `docs/PRD-*.md` |
| 1 | **feature-decomposer** | 有明确的 PRD 或需求描述 | PRD 或用户需求 | `docs/architecture/product-spec.md` + `docs/test/requirements-traceability.md` |
| 2a | **code-reviewer**（阶段一·架构评审） | feature-decomposer 出方案后、coder 动手前 | 产品规格 + 架构方案 | `docs/architecture/architecture-review.md` |
| 2b | **frontend-page-designer** | 有 UI/页面相关的功能需要设计 | 功能清单 | 设计规格 |
| 3 | **module-coder** | 架构评审通过、功能清单已确认 | 功能清单 + 设计规格 | 代码实现 + milestone 报告 |
| 4a | **code-reviewer**（阶段二·代码评审） | coder 到达里程碑后、QA 介入前 | 代码 + 里程碑报告 + 追踪清单 | `docs/test/code-review-M{N}.md` |
| 4b | **design-parity-inspector** | coder 实现了有 UI 的功能 | 代码 + 设计规格 | 设计一致性报告 |
| 5 | **qa-inspector** | code-reviewer 阶段二通过后、里程碑节点 | 代码 + 追踪清单 + 各类评审报告 | QA 报告 |

### 大型需求（新功能、新页面、重构等）

按需从以下步骤开始：
- **需求模糊时**：从 product-manager 开始 → 完整走完 0 → 1 → 2a → 2b → 3 → 4a → 4b → 5
- **需求明确时**：从 feature-decomposer 开始 → 1 → 2a → 2b → 3 → 4a → 4b → 5
- **纯后端/无 UI**：跳过 2b 和 4b（页面设计与设计还原审核） → 1 → 2a → 3 → 4a → 5
- **架构评审（2a）发现 P0/P1**：回退给 feature-decomposer 修方案，再次评审通过后才能进入实现
- **代码评审（4a）发现 P0**：coder 立即修复；**P1/P2**：coder 修复后必须重新走一遍 qa-inspector；**仅 P3**：放行进入 4b/5

### 小型任务（bug 修复、小改动等）
1. **module-coder** — 开始开发修复
2. **code-reviewer**（阶段二）— 涉及安全 / 数据库 / 分布式逻辑的改动必跑；纯样式 / 文案改动可跳过
3. **qa-inspector** — 测试验收

### 流水线协作规范

- 每个 agent 的输出文档是下一个 agent 的输入，**必须写入 `docs/` 目录**
- `docs/requirements-traceability.md` 是全链路的"对账底本"，从 feature-decomposer 生成，module-coder 和 qa-inspector 持续更新
- 上游 agent 完成后，自动启动下一个 agent，不等用户指令
- 如果某阶段发现上游问题（如 PRD 缺失关键信息），可以回溯到上游 agent 补充

**绝对不要跳过这个流程，不要等用户提醒，收到任务就直接启动对应流水线。**
